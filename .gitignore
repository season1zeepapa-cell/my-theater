# Vercel 배포 설정 (로컬에서만 사용)
.vercel

# Node.js 의존성 폴더 (npm install로 다시 설치 가능)
node_modules/

# Mac 시스템 파일 (자동 생성되는 숨김 파일)
.DS_Store

# 환경 변수 파일 (비밀 정보가 들어있을 수 있음)
.env
.env.local
.env*.local

# 로그 파일
*.log
npm-debug.log*

# 에디터 설정 파일
.vscode/
.idea/

# 빌드 output
dist/
